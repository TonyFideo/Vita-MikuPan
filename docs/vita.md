# Vita

This branch starts the PS Vita port.

The first build only checks the VitaSDK, VitaGL and ARM32 base. It opens a 960x544 VitaGL screen with MSAA disabled. Press START to exit.

Game code, audio and the full renderer will be connected in later steps.
